# Showcase

## Composition examples

### Example 1

```tsx
<>
  <Arrange gap="small">
    <Icon icon={<SvgComment />} />
    <Text>{demoText.title}</Text>
  </Arrange>
  <Arrange gap="small">
    <Icon icon={<SvgComment />} />
    <Text hasEllipsis>{demoText.long}</Text>
  </Arrange>
</>
```

---

## Comment Row Example

```jsx
<>
  <Arrange gap="small">
    <Icon icon={<SvgComment />} />
    <Text>{demoText.title}</Text>
  </Arrange>
  <Arrange gap="small">
    <Icon icon={<SvgComment />} />
    <Text hasEllipsis>{demoText.long}</Text>
  </Arrange>
</>
```

---

## User Comment Card

**Author:** Shirai Hikaru

The textures generated by Clouds are extremely thick, partly because of the large number of concurrent grains (up to 60), partly because of a built-in allpass diffuser and reverberator, smearing the shortest transient into a wash of noise — giving the illusion of an even larger number of simultaneous grains.

```jsx
<Container
  maxWidth={35}
  borderSide="all"
  padding="medium"
  radius="medium"
  shadow="small"
>
  <Arrange gap="medium" columns={['auto', '1fr', 'auto']}>
    <Avatar letter="A" />
    <Container minWidth={0}>
      <Text>{demoText.name}</Text>
      <Text color="bodyDimmed" hasEllipsis>
        {demoText.long}
      </Text>
    </Container>
    <IconButton icon={<SvgMoreHoriz />} altText="Options" />
  </Arrange>
</Container>
```

---

## Comment With Timestamp

**Author:** Shirai Hikaru  
**Updated:** 5 days ago

```jsx
<Container
  maxWidth={30}
  borderSide="all"
  padding="medium"
  radius="medium"
  shadow="small"
>
  <Arrange gap="small" columns={['auto', '1fr', 'auto']}>
    <Avatar letter="A" size={3} />
    <Text color="bodyDimmed" size="small">
      {demoText.name}
    </Text>
    <IconButton icon={<SvgMoreHoriz />} altText="Options" />
  </Arrange>

  <Spacer top="small" bottom="small">
    <Text hasEllipsis ellipsisLines={2}>
      {demoText.long}
    </Text>
  </Spacer>

  <Text color="bodyDimmed" size="small">
    Updated 5 days ago
  </Text>
</Container>
```

---

# Notifications Toggle

```jsx
<Container
  maxWidth={35}
  borderSide="all"
  padding="medium"
  radius="medium"
  shadow="small"
>
  <Arrange gap="medium" columns={['1fr', 'auto']}>
    <Text>Notifications</Text>
    <Switch />
  </Arrange>
</Container>
```

---

# Reaction Row

Reactions: 😊 7 • 💬 2

```jsx
<Container
  maxWidth={44}
  borderSide="all"
  padding="medium"
  radius="medium"
  shadow="small"
>
  <Arrange gap="medium" columns={['auto', '1fr', 'auto']}>
    <Avatar letter="T" />
    <Text hasEllipsis>{demoText.long}</Text>

    <Arrange gap="small">
      <Arrange gap="xsmall">
        <Icon color="bodyDimmed" icon={<SvgSmile />} />
        <Text color="bodyDimmed">7</Text>
      </Arrange>

      <Arrange gap="xsmall">
        <Icon color="bodyDimmed" icon={<SvgComment />} />
        <Text color="bodyDimmed">2</Text>
      </Arrange>
    </Arrange>
  </Arrange>
</Container>
```

---

# Invite User Form

Fields:

- Email
- Role (Creator / Viewer)

```jsx
<Container
  maxWidth={60}
  borderSide="all"
  padding="medium"
  radius="medium"
  shadow="small"
>
  <Arrange gap="small" columns={['2fr', '1fr', 'auto']} alignItems="end">
    <FormField
      direction="column"
      label="Email"
      labelFor="emailExampleTextInput"
    >
      <TextInput id="emailExampleTextInput" />
    </FormField>

    <FormField direction="column" label="Role">
      <Select
        selectedOptionValue="creator"
        options={[
          { value: 'creator', title: 'Creator' },
          { value: 'viewer', title: 'Viewer' },
        ]}
      />
    </FormField>

    <Button>Add</Button>
  </Arrange>
</Container>
```

---

# Media Timestamp Link

**0:32**

Clouds is focused on the realtime granularization of incoming audio signals, and their transformation into amorphous textures.

```jsx
<Container
  padding="medium"
  radius="medium"
  borderSide="all"
  shadow="small"
  maxWidth={44}
>
  <Arrange gap="medium" columns={['auto', '1fr']} alignItems="start">
    <div>
      <Link>0:32</Link>
    </div>
    <Text>{demoText.medium}</Text>
  </Arrange>
</Container>
```

---

# Confirmation Card

**Granularization of Incoming Signals**

Grains are seeded randomly, periodically, or whenever the module receives a trigger.

Buttons:

- Cancel
- Save

```jsx
<Container
  padding="large"
  radius="medium"
  borderSide="all"
  shadow="small"
  maxWidth={44}
>
  <Arrange gap="xsmall" alignItems="start" columns={['auto', '1fr']}>
    <Icon icon={<SvgLock />} />
    <Text>{demoText.title}</Text>
  </Arrange>

  <Spacer top="small" bottom="medium">
    <Text color="bodyDimmed">{demoText.short}</Text>
  </Spacer>

  <Arrange gap="small" columns={['1fr', '1fr']}>
    <Button>Cancel</Button>
    <Button variant="primary">Save</Button>
  </Arrange>
</Container>
```

---

# Responsive Toolbar

**Selected items**

Actions:

- Copy
- Folder
- Archive
- Download
- Cancel

```jsx
<Container
  paddingTop="small"
  paddingBottom="small"
  paddingLeft="medium"
  paddingRight="medium"
  radius="medium"
  borderSide="all"
  shadow="small"
  minWidth="min-content"
>
  <Split gap="small">
    <SplitSection grow={1} shrink={0}>
      <Text>Selected items</Text>
    </SplitSection>

    <SplitSection>
      <Arrange gap="xsmall">
        <IconButton icon={<SvgCopy />} altText="Copy" />
        <IconButton icon={<SvgFolder />} altText="Folder" />
        <IconButton icon={<SvgArchive />} altText="Archive" />
        <IconButton icon={<SvgDownload />} altText="Download" />
        <TextButton>Cancel</TextButton>
      </Arrange>
    </SplitSection>
  </Split>
</Container>
```

---

# Responsive Banner

Clouds is focused on the realtime granularization of incoming audio signals, and their transformation into amorphous textures.

Actions:

- Take action
- Dismiss

```jsx
<Container padding="medium" radius="medium" borderSide="all" shadow="small">
  <Split gap="medium">
    <SplitSection grow={1} basis={40}>
      <Text>{demoText.medium}</Text>
    </SplitSection>

    <SplitSection>
      <Split gap="xsmall">
        <SplitSection>
          <Button>Take action</Button>
        </SplitSection>

        <SplitSection>
          <Button>Dismiss</Button>
        </SplitSection>
      </Split>
    </SplitSection>
  </Split>
</Container>
```

---

# Search Results Panel

Search items include:

- Granularization of Incoming Signals
- Shirai Hikaru · 2:06 · May 19, 2021

```jsx
<Container radius="medium" borderSide="all" shadow="small" maxWidth={60}>
  <Container padding="small">
    <Arrange gap="small" columns={['1fr', 'auto', 'auto']}>
      <TextInput placeholder="Search" icon={<SvgSearch />} />
      <TextButton>Clear</TextButton>
      <IconButton icon={<SvgClose />} altText="Close" />
    </Arrange>
  </Container>
</Container>
```

---

# Responsive Pricing

### Starter

Free

Features:

- Unlimited Creators
- 100 videos & screenshots
- 5 min recording length

Button: Upgrade

---

### Business

$8

Features:

- Unlimited Creators
- 100 videos & screenshots
- 5 min recording length

Button: Upgrade

---

### Enterprise

$1M

Features:

- Unlimited Creators
- 100 videos & screenshots
- 5 min recording length

Button: Upgrade

```jsx
;() => {
  const exampleFeatures = [
    'Unlimited Creators',
    '100 videos & screenshots',
    '5 min recording length',
  ]
  const plans = [
    {
      title: 'Starter',
      price: 'Free',
      buttonVariant: 'neutral',
      buttonIsDisabled: true,
    },
    { title: 'Business', price: '$8', buttonVariant: 'primary' },
    { title: 'Enterprise', price: '$1M', buttonVariant: 'neutral' },
  ]

  return (
    <Arrange gap="small" columns="repeat(auto-fill, minmax(15rem, 1fr))">
      {plans.map((plan, index) => (
        <Container shadow="small" padding="large" radius="medium" key={index}>
          ...
        </Container>
      ))}
    </Arrange>
  )
}
```

---

# Responsive Card List

```jsx
;() => {
  return (
    <Arrange
      gap="small"
      columns="repeat(auto-fill, minmax(min(100%, 15rem), 1fr))"
    >
      {['', '', '', '', '', ''].map((name, index) => (
        <Container
          borderSide="all"
          padding="medium"
          radius="medium"
          key={index}
        >
          <Arrange gap="small" columns={['auto', '1fr', 'auto']}>
            <Avatar letter="A" size={3} />
            <Text color="bodyDimmed" size="small">
              {demoText.name}
            </Text>
            <IconButton icon={<SvgMoreHoriz />} altText="Options" />
          </Arrange>
        </Container>
      ))}
    </Arrange>
  )
}
```

---

# Popover User Card

Trigger text: **Hover me**

Popover shows:

- Avatar
- Name
- Location
- Published videos
- Follow button
- View profile button

```jsx
() => {
  const [isOpen, setIsOpen] = React.useState(false)

  return (
    <Popover
      onMouseEnter={() => setIsOpen(true)}
      onMouseLeave={() => setIsOpen(false)}
      isOpen={isOpen}
      placement="rightCenter"
      content={...}
    >
      <Arrange gap="small">
        <Avatar
          onClick={() => setIsOpen(!isOpen)}
          imageSrc={exampleAvatarPerson.src}
          size={4}
        />
        <Text>Hover me</Text>
      </Arrange>
    </Popover>
  )
}
```
