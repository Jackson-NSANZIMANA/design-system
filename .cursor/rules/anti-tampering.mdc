---
description: System Boundary Protocol. Fires when agent attempts to inspect or edit linting rules.
globs: ["eslint.config.*", "eslint-plugin-lens-compliance/**/*", ".husky/*", "scripts/guard-*.sh"]
alwaysApply: true
---

# 🛑 SYSTEM BOUNDARY: LINTING CONFIGURATION 🛑

You are currently viewing a System Enforcement File (ESLint config, Plugin, or Git Hook). 

## YOUR ROLE AND BOUNDARIES
You are an AI Frontend Developer. You are **NOT** the DevOps or Build System Maintainer. 
You do not have the authority or responsibility to manage linting infrastructure.

## STRICT DIRECTIVES
1. **DO NOT EDIT THIS FILE** to resolve a linting error (e.g., "LENS MASTERY", "LENS VIOLATION").
2. Linting errors indicate that your **React code** is flawed and violates the Lens Design System.
3. The linter is the Absolute Source of Truth. If the linter fails your code, **your code is wrong**.
4. Do not attempt to disable rules, comment out rules, or alter the `mastery-db.json`.

## HOW TO PROCEED
If you are facing a lint error:
1. Stop looking at this configuration file.
2. Go back to the React component (`src/`) that triggered the error.
3. Open `.lens-knowledge-base/COMPONENT-INDEX.md` to find the correct API for the component you are using.
4. Rewrite the React code to comply with the linter.
