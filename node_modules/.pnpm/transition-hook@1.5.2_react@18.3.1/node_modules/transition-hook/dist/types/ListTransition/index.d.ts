import React from 'react';
import { Stage } from '..';
declare type ListTransitionProps<Item> = {
    list: Array<Item>;
    timeout: number;
    children: (item: Item, stage: Stage) => React.ReactNode;
};
export declare function ListTransition<Item>({ list, timeout, children, }: ListTransitionProps<Item>): JSX.Element[];
export {};
