"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useDefaultMode = void 0;
const react_1 = require("react");
function useDefaultMode({ state, timeout, mode, keyRef, list, setList, }) {
    (0, react_1.useEffect)(() => {
        // skip unmatched mode 🚫
        if (mode !== undefined && mode !== 'default')
            return;
        // skip fist mount and any unchanged effect 🚫
        const [lastItem] = list.slice(-1);
        if (lastItem.state === state)
            return;
        // 0 update key
        const prevKey = keyRef.current; // save prev key
        keyRef.current++; // update to last item key
        const curKey = keyRef.current; // save cur key (for async gets)
        // 1 add new item immediately with stage 'from'
        setList((prev) => prev.concat({ state, key: curKey, stage: 'from' }));
        // 1.1 change this item immediately with stage 'enter'
        const isCurItem = (item) => item.key === curKey;
        setTimeout(() => {
            setList((prev) => prev.map((item) => (isCurItem(item) ? Object.assign(Object.assign({}, item), { stage: 'enter' }) : item)));
        });
        // 1.2 leave prev item immediately with stage 'leave'
        const shouldItemLeave = (item) => item.key === prevKey;
        setList((prev) => prev.map((item) => shouldItemLeave(item) ? Object.assign(Object.assign({}, item), { stage: 'leave' }) : item));
        // 2 unmount leaved item after timeout
        const shouldMountItem = (item) => item.key !== prevKey;
        setTimeout(() => {
            setList((prev) => prev.filter(shouldMountItem));
        }, timeout);
    }, [keyRef, list, mode, setList, state, timeout]);
}
exports.useDefaultMode = useDefaultMode;
