/// <reference types="react" />
import { Stage } from '../useTransition';
export declare type Mode = 'default' | 'out-in' | 'in-out';
export declare type ListItem<S> = {
    state: S;
    key: number;
    stage: Stage;
};
export declare type ModeHookParam<S> = {
    state: S;
    timeout: number;
    mode?: Mode;
    keyRef: React.MutableRefObject<number>;
    list: ListItem<S>[];
    setList: React.Dispatch<React.SetStateAction<ListItem<S>[]>>;
};
