/// <reference types="react" />
import { Stage } from '../useTransition';
import { Mode } from './types';
declare type RenderCallback<S> = (state: S, stage: Stage) => React.ReactNode;
export declare function useSwitchTransition<S>(state: S, timeout: number, mode?: Mode): (renderCallback: RenderCallback<S>) => JSX.Element[];
export {};
