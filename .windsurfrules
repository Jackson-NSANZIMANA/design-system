# Windsurf — Lens Design System

## Canonical Rules
All rules, prohibitions, and governance live in:
→ CLAUDE.md (read this first, always)

## Lens Knowledge Base  
→ .lens-knowledge-base/_ROUTER.md (find any file)

## Windsurf-Specific Notes

### Before any UI task
1. Read CLAUDE.md
2. Read .lens-knowledge-base/guides/LENS_THINKING.md
3. Route via .lens-knowledge-base/_ROUTER.md

### Key Lens Differences
- Layout: Container (not Box), Split (not Flex), Arrange (not Grid)
- Text: Text component always (not h1/p/span)
- Booleans: isDisabled, hasLoader (not disabled, loading)
- Element: htmlTag="a" (not as="a")
- Refs: refHandler={(r) => ref.current = r} (not ref={})
- Icons: Icon icon={SvgName /} with Svg prefix
- Spacing: named tokens xsmall/small/medium/large/xlarge/xxlarge
- Button icons: iconBefore/iconAfter (not leftIcon/rightIcon)

### Styling Rules (CORRECTED)
- className with Lens utility classes: ALLOWED on raw wrappers
- className with Lens utilities on Lens components:
  external spacing only (mr:small, mt:medium)
- CSS modules with var(--lns-*): ALLOWED for custom wrappers
- style={{}}: NEVER allowed — this is the only absolute
- Tailwind classes: NEVER allowed

### Token Reference
→ .lens-knowledge-base/tokens/_tokens-index.md
→ NEVER: reference/styles-full.md (7,000 lines, wastes context)