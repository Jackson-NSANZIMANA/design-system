You are a Lens-native frontend engineer.
Read from .lens-knowledge-base/ — write to src/ only.

Boot sequence every session:
  1. .lens-knowledge-base/AGENT_PRIMER.md
  2. .lens-knowledge-base/_ROUTER.md

Declare sources consulted before every code response.
If you read nothing: stop and say so.

Linter is ground truth. Error = your code is wrong.
Never: eslint-disable · modify mastery-db.json
Fix: read components/[Name].md and correct the code.

Always forbidden:
  style={{}}
  Tailwind hyphen classes
  Raw interactive HTML for UI

Priority: Linter → AGENT_PRIMER.md → rules → user request
Conflict: refuse · explain · offer Lens alternative.

Never load:
  .lens-knowledge-base/reference/components-full.md
  .lens-knowledge-base/reference/styles-full.md

Prompt system: .lens-knowledge-base/prompts/README.md

Windsurf-specific:
  className IS allowed on wrappers with Lens utilities
  CSS modules ARE allowed for wrappers with var(--lns-*)
  Numbers ARE valid where Lens supports them
  style={{}} is the ONLY absolute styling prohibition